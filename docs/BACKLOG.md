# BACKLOG.md — 决策与待办台账（AI 必读，每次会话开头接续）

> 本文件是项目"决策历史 + 待办状态"的唯一权威（AGENTS.md 强制引用）。
> 新会话开工前必须读它：已裁决事项**不得推翻**，待决事项**接续决策**。
> 更新规则：任何裁决/实施完成/状态变化，必须同步更新本文件。

## 0. 当前状态速览

- 所有已识别 bug 除下表标注"待决/已关闭"外均已修复（见 §1 已完成）
- 未决事项：D4（防抖丢写窗口）—— 待产品拍板
- 演进项（触发条件满足才做，禁止提前重构）：见 §3

## 1. 已裁决并实施（含 commit）

| 事项 | 裁决 | 提交 |
|---|---|---|
| 顶栏导出用排样快照（历史方案导出不随当前零件表漂移） | 修 | `15f72ae` |
| 零件编辑持久化防抖（500ms 拖尾，防大表逐键写 IndexedDB 卡顿） | 修 | `053ed0d` |
| 历史列表刷新与落库绑定（historyRev），修复展开时计算后不显示 | 修 | `f4a5e86` |
| 历史方案点击即查看 + 一键继续编辑（快照零件/板材/设置恢复） | 做 | `2abcf3b` |
| dirty 改为派生状态（方案输入指纹比较）——失败/取消/运行中修改均正确提示 | 修 | `5bc2a48` |
| dirty 指纹变化联动重算（修计算成功/无方案时恒显"有改动未计算"） | 修 | `8a95420` |
| 方案 id/createdAt 分配收归 storage.savePlan 单一实体化（删死代码） | 重构 | `0470208` |
| 计算错误归一化单一入口（normalizeOptimizeError）+ 类型收窄，fallback 不丢业务码 | 修 | `8ebb51f` |
| 取消信号用标准 throwIfAborted；ARCHITECTURE §4.1 澄清 WHATWG 标准接口允许 | 修 | `8feb4f0` |
| 切割图选中/悬停联动（图上高亮/变暗/跨板跳页/空白清空） | 修 | `bfa3da0`~`bfeea05`（6 个 commit） |
| D1 历史视图顶栏导出被 dirty 误伤 | 随 dirty 派生化自动修复（`8a95420`） | — |
| **零件数量整数化（浮点 quantity 从源头杜绝）**：Part.quantity 品牌类型 `Quantity`（唯一生产点 `qty()` 截断法，禁止 `as Quantity`）；全入口统一 qty 语义（工作区/批量粘贴/CSV/OCR 导入：2.9→2、0.4→0，不报错；0 = 不参与计算的显式状态）；storage v4 迁移清洗存量；optimize 直接拿整数实例（类型保证，无运行时防御） | 修 | `3930839` |

## 2. 已裁决、待决或已关闭

| 事项 | 状态 | 裁决/说明 | 触发条件（若未实施） |
|---|---|---|---|
| D2 继续编辑覆盖板材库（historyActions 整体替换，已删规格可能"写复活"） | **已裁决（产品）**：采用 A 覆盖语义——"继续编辑=恢复该方案完整输入态"，不做确认弹窗；未来用**动画**让变更可见（板库面板高亮/「历史」来源标记） | 动画可见性 → UI 迭代阶段（backlog §3） | — |
| D3 旧记录（无 parts 快照）重建零件表丢 grain/sheetId/edgeBand | **已关闭**：开发初期无真实用户，兼容债不存在；新记录恒完整 | 首次真实发布前确认数据基线（清空/迁移评估） | 发布准备阶段 |
| D4 防抖丢写窗口（500ms 内关闭应用，最后编辑未落库） | **待决**：a) 现在做 flush（pagehide/beforeunload + pendingSaves 存快照 + 架构文档 §8.1 补持久化时机约定）b) 推迟到 Tauri 阶段（Rust close-requested 拦截 + flush） | 见上两选项 | 待产品拍板 |
| #5 specOf 静默兜底 vs toScene 抛错（4 处行为不一致） | **待确认**：方案 Y 注释互引（记录"UI 兜底为防御死代码、导出一律抛错"契约）；或直接关闭（触发面=数据损坏，正常流程不可达） | — | 待确认 |
| 板材规格内嵌（SheetLayout.spec 替代 sheetSpecId 引用） | **已裁决**：不做（资深评审：收益与成本不成比例、内嵌引入新同步义务、正常流程不可达）；收敛为单一 `sheetSpecOf` 函数也不做（同为防御死代码） | 出现"方案编辑/单板覆盖规格/方案对比"等真实功能需求 | 功能驱动，禁止提前重构 |
| 方案主键指纹化（PlanRecord.id → 指纹主键） | **已裁决**：保持 UUID 主键；指纹主键为 v2 演进项（需 sha-256 升级 + createdAt 语义决策 + schema 迁移） | schema 迁移窗口 + 时间线需求明确 | v2 |

## 3. 演进项（触发条件满足才做，禁止提前重构）

| 演进项 | 触发条件 |
|---|---|
| D2 板库变更动画可见性（继续编辑落位后板库面板高亮/来源标记） | UI 迭代阶段 |
| 板材规格内嵌 SheetLayout.spec | 方案编辑/对比等真实功能需求 |
| 方案主键指纹化 | schema 迁移窗口 + 时间线需求明确 |
| D4 flush 出口（Tauri 侧 close-requested 拦截 + 等待落库） | Tauri 打包阶段（阶段 7） |

## 4. 已确认不做（避免重复提出）

- 方案 id 的 `Math.random` 死代码兜底：已删除（0470208），未来任何新入口都不该再写兜底 id
- 无 Worker fallback 与 Worker 路径错误码对称：已修（8ebb51f），两路径共用 normalizeOptimizeError
- 评价函数字典序规则（evaluate.ts）：任何改动必须问用户确认（AGENTS.md §4.3）
