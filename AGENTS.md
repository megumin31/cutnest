# AGENTS.md — 项目工作手册（AI 必读）

> 你是本项目的一个 AI 开发会话。**开工前先读完本文件 + `docs/ARCHITECTURE.md`**，本文档描述"怎么在这个项目里干活"，架构文档描述"系统长什么样"。两者冲突时，先改文档再改代码。**涉及 UI 视觉时，还必须读 `docs/UI-DESIGN.md`**（色彩/字体/布局/切割图可视化规范）。**产品/技术决策与待办状态见 `docs/BACKLOG.md`**（含各问题裁决、演进项及触发条件——新会话必须先读它再接续，避免决策丢失或推翻）。

## 1. 项目一句话

木工裁板优化商业软件：本地计算最优排布（用板最少、余料最集中），输出 PDF（给客户看）和 DXF（给机器执行），桌面端买断制 + AI 手写识别增值服务（按次扣费）。

## 2. 技术栈速查

React + TypeScript(strict) + Vite | Ant Design | Zustand | Dexie(IndexedDB) | Tauri 2 | Vitest
服务端：Cloudflare Pages + Workers + D1（SQLite）| 账号：邮箱注册 + Google OAuth | AI：OCR 供应商抽象（默认 Qwen-OCR，可切换）

## 3. 你被要求做什么之前，先确认

- 用户没有说"开始写代码"，就不要创建 todo、不要改文件。先讨论清楚。
- 多步骤任务：先 `todowrite` 建清单，一步一标记。
- 领域问题不确定时，**问用户**——木工行业术语（长/宽、切缝、余料、满长件）有精确含义，不要猜。

## 4. 硬性规则（违反即返工）

**系统级硬规则以 `docs/ARCHITECTURE.md` §4 为唯一权威**（两篇文档冲突时架构文档优先），包括：domain 纯净（禁 React/浏览器 API）、模块 index.ts 契约、mm 尺寸整数 + 工艺参数可小数（epsilon=0.001）、platform 适配器禁平台分支、接口先行、排样过 validator、固定种子确定性、store 只存会话态、三大模块配单测、schema 迁移函数。

**以下为工作纪律（本文件特有，架构文档不重复）：**

1. 禁止 `as any`、`@ts-ignore`、空 catch —— 类型安全是底线
2. 修 bug 要最小改动，禁止顺手重构
3. 评价函数（evaluate.ts）字典序规则的任何改动，必须问用户确认——产品价值观，不是技术细节

## 5. 领域术语与约定（说错就做错）

| 术语 | 正确理解 |
|---|---|
| 长 × 宽 | 零件/板材一律"长×宽"，**不是**宽×高。长度/宽度是**方向标签**（长度边沿 X 轴、宽度边沿 Y 轴），不要求 `length ≥ width`；板材与零件同语义 |
| grain | `'alongLength'` = 禁止旋转（**默认**；UI 呈现为「旋转」勾选，不勾选即此值）；`'any'` = 可旋转（勾选即生效） |
| 切缝 kerf | 统一参数：精密锯锯缝 / 雕刻机刀径共用一个字段，**默认 3mm**（用户可在设置中覆盖）；**零件间净距 = kerf**（一刀切透场景，无额外安全间距） |
| 满长件 | 长度 = 板材长度的部件（如 2440×400），自由排样天然适配，无需特殊处理 |
| 板材库 | 项目可用的板材规格集合（多选 ≥1）；排样时每张新板选「能装下当前零件、面积最小」的规格；评价只看物理张数，不看规格价格 |
| 指定板材 | 零件可指定 `sheetId`（板材库中规格）：指定件定板型（只能进指定规格的板），未指定（任意）的零件可混入任何板 |
| 余料集中 | 评价规则：可再利用余料（≥ minReusableWaste，默认 200×200）块数越少越好、最大块越大越好；碎料算废料 |
| 字典序评价 | **先用板张数最少，再同类聚排（同尺寸零件整排聚块），再余料集中**——顺序不可颠倒，这是产品价值观；评价函数唯一，不可配置 |
| 排样方向 | 零件向板材一端挤，余料集中在尾部成整块（skyline 堆叠时遵守） |
| PDF 用途 | 给人看：客户交付/沟通，标注+统计+水印 |
| DXF 用途 | 给机器执行：闭合轮廓 + 切割顺序组织 + 空行程优化（最近邻+2-opt），轮廓统一方向，起点在角上 |
| 账号即身份 | 账号系统！邮箱注册 + Google 登录，买断标记与识别次数挂账号；**同一账号最多同时登录 3 台设备**（滚动淘汰，见 ARCHITECTURE §9） |
| 审查表 | AI 识别结果的人工修正界面：AI 填的格子高亮，用户逐格确认后导入 |

## 6. 新模块标准工作流（每次都要走）

1. 在 `domain/types.ts` 或模块内定义**类型/接口**（接口即任务说明书）
2. 实现为**纯函数**（可测试、无副作用、无 I/O）
3. 写 **Vitest 单测**（含边界：空输入、单零件、超大零件、数量=0、旋转冲突）
4. 用 `lsp_diagnostics` 检查类型错误，跑测试确认通过
5. 改完汇报：改了什么、在哪、验证了什么

## 7. 算法模块专项要求（optimizer / exporter）

- **基准测试**：`tests/fixtures/benchmark.ts` 500 零件基准集（总面积 ≈ 20 张板量级），性能改动前后必须跑。验收：**<15s + 利用率 ≥ 85% + 确定性 + 零件完整性**（见 ARCHITECTURE §6.2）
- **确定性测试**：同输入跑两次结果必须一致
- **validator 测试**：必须能拦截重叠、越界、切缝不足三类错误方案
- 算法输出格式变更 = 破坏性变更，必须先更新架构文档再动手
- 评价函数（evaluate.ts）的字典序规则：任何改动都要问用户确认——这是产品价值观，不是技术细节

## 8. 常用命令（项目搭建后生效）

```bash
npm run dev          # 启动 Vite dev（Web 端）
npm test             # Vitest 单测
npm run tauri dev    # 启动桌面端（需 Rust 工具链）
npm run build        # 构建 Web 产物
npx tauri build      # 打包桌面安装包
```

## 9. 沟通约定

- 用户用中文交流，回复用中文；代码、类型、注释用英文
- 大白话优先，少掉技术术语书袋（团队不写代码）
- 不确定业务语义（木工工艺细节）时，先问，不要假设
- 发现架构文档与代码不一致：停下来，先问用户改哪边

## 10. 当前状态

- 设计定稿 v1.0（2026-08 评审修订版，见 `docs/ARCHITECTURE.md`）
- **已实现（阶段 1~6）**：domain 全模块（types/units/materials/pricing/optimizer/exporter）含单测与 500 零件基准（<6s、利用率 91.9%、确定性、过 validator）；storage（Dexie v1）；worker（消息协议 + 取消/进度）；UI 全部页面（项目列表/工作台三栏/审查表/账号/设置/i18n zh+en/主题）；导出 PDF（含 CJK 子集化）/ DXF（含空行程优化）
- **未开工（阶段 7~10）**：src-tauri 打包、functions 服务端（auth/oauth/heartbeat/recognize/buy）、recognition 真实 OCR 对接（当前 mock）、其余 11 门语言翻译
- v1.0 评审修订要点：授权体系定为**账号系统**（邮箱 + Google 登录，3 台设备滚动淘汰，§9）；OCR 供应商抽象；性能验收加利用率指标；免费/付费体验边界（§2）；PDF 13 语言国际化
- 待确认项：异形件 v2（不阻塞开工）
- **决策台账**：全部问题裁决/演进项/触发条件见 `docs/BACKLOG.md`（必读，防止决策丢失或推翻）
